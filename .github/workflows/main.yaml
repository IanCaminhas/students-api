name: Students API Pipeline

on:
  push:
    branch: main

jobs:
  hello:
    runs-on: windows-latest

    steps:
      - name: Say Hello
        run: echo "Hello World"

  
   listing:
    runs-on:  windows-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
      - name: List all files
        run: ls